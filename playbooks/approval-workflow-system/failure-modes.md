# Approval Workflow Failure Modes

- Unauthorized decisions -> governance breach -> explicit authorization checks.
- Escalation drift -> delayed outcomes -> timer policy validation.
- Duplicate callbacks -> downstream inconsistency -> idempotent callback processing.
- Hidden backlog -> SLA misses -> queue monitoring and alerting.
